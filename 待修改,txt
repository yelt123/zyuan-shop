1, 
    navbar组件的返回上一层，现在用的是简单的一个判断，在EditAddress组件里，效果不是很好

history.back()




2, 
    Address组件里把当前点击的信息传输给EditAddress组件文本框里

vant 组件内有个回调 可以返回这个值 



3，
    在订单组件中，鼠标快到底部时再次获取后面的订单数据

判断srcoll滚动条到达底部时 重现发送请求，页数加一，获得的值push到数组后面



4, 
    选取头像文件获取选中的数据

  vant有个返回值



5,
    搜索后重新渲染模板
    
取消keepalive

6,
    loading组件何时加载

    在created中就创建这个组件给其状态true，然后获取数据成功时，切换状态


    192.168.0.35